<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="radial"
                android:gradientRadius="600dp"
                android:centerX="50%"
                android:centerY="50%"
                android:startColor="@color/poker_green"
                android:endColor="@color/poker_green_dark" />
        </shape>
    </item>
    <!-- We could add a bitmap tile for felt texture here if we had the resource -->
</layer-list>
